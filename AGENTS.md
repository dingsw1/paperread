# AGENTS.md

## 通用规范（所有项目共享）

- 运行时与包管理：JS/TS 默认使用 bun，Python 默认使用 uv。
- 软件源：尽量设置为中国大陆的镜像。
- 依赖选择：仅允许使用商业友好的第三方库；如需例外，须先征求用户同意并说明原因。
- 版本管理：遵循语义化版本（SemVer），版本变更需同步记录到变更日志。
- 文档驱动：维护 BACKLOG.md、CHECKLIST.md、CHANGELOG.md、RELEASE-NOTE.md 等过程与发布文档。
- 代码质量：提交前必须通过 lint 与编译，保持 0 warning。
- Git 规范：提交消息遵循 Conventional Commits；标题遵守 52/70 字符限制；正文表达 what/why 而非 how，并使用空行与 bulleted list。
- 测试要求：新增或修改逻辑必须配套测试；测试失败不得绕过或删除。
- 运行效率：充分利用多核和多 GPU 资源，并行处理大量数据，但要避免 GPU 使用冲突。
- 安全与合规：不得提交密钥或凭证类文件；涉及安全风险须主动提示。
- 失败处理：困难问题连续 3 次尝试仍未解决，应停止并向人类报告。